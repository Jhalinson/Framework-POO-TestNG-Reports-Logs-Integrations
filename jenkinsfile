pipeline{
  agent any
    stages{
      stage("clean"){
        steps{
          mvn clean
    }
    stages{
      stage("build"){
        steps{
          mvn build
    }
    stages{
      stage("test"){
        steps{
        mvn install
    }
  
  }
}
}

maven{
  
}
